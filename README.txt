Set List PWA v1.23

Fix for Current Set:
- Only the song count / total time bar is sticky while scrolling.
- The saved-set name is explicitly non-sticky and scrolls away with the list.
- Spacing between Total and the saved-set name is reduced to a compact 6 px visual gap.
- All v1.22 functionality and stored data remain unchanged.
- Caching remains disabled during development.
